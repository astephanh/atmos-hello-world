import:
  - org/acme/_defaults

vars:
  tenant: org
  environment: acme
  stage: test

components:
  terraform:
    vpc:
      metadata:
        component: vpc/v0.1.0
    hello-world:
      metadata:
        component: hello-world/v0.1.0
