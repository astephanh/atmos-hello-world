import:
  - org/acme/_defaults

settings:
  tenant: org
  environment: acme
  stage: test

components:
  terraform:
    hello-world:
      metadata:
        component: hello-world/v0.1.0
      vars:
        lang: de
        location: hh
        region: hh
